import styles from 'src/styles/ImageTagging.module.css';

const InternalServerError = () => {
  return <div className={`${styles.font}`}>Internal Server Error</div>;
}

export default InternalServerError;